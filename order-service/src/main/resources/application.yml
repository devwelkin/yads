server:
  port: 8083

spring:
  application:
    name: order-service


  datasource:
    url: jdbc:postgresql://localhost:5434/order_service_db
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update


  rabbitmq:
    host: localhost
    port: 5672

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/senin-realm-adin # D??ERLER?YLE AYNI OLMALI

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/